import React from 'react';

const PlayerSetup = ({ onStart }) => {
  // Placeholder UI for player setup
  return (
    <div>
      <h2>Player Setup</h2>
      {/* TODO: Add player name inputs and start button */}
      <button onClick={onStart}>Start Game</button>
    </div>
  );
};

export default PlayerSetup;
